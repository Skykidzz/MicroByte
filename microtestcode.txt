MICRODE--
VAR y = 54;
VAR v = 15;
VAR d = 0;
VAR e = true;

//makes the loop move with delta time
deltaLoops(true)

loop() {
    var d = deltaTime();
    if(e == true) {
    var y += v * d;
    var v -= 0.5;
    }
    rect(20,y,20,20)
    frameRate(60)
}
loop(60) {
    frameRate(30)
}